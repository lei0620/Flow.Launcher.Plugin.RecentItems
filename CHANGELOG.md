# 更新记录

## 1.2.0

- 插件显示名称改为 `History Box`，保持插件 ID 不变。
- 恢复插件自己的持久置顶与取消置顶，不显示成功提示，并真正固定到主页列表顶部。
- 文件夹继续通过整行操作直接打开，插件菜单不再显示“打开所在位置”。
- 普通文件提供“打开所在位置”，并在文件资源管理器中选中文件。
- 置顶和取消置顶分别使用 Lucide `pin`、`pin-off` 图标。

## 1.1.1

- 移除与 Flow 内置“在当前查询中置顶”重复的插件置顶操作。
- “打开所在位置”改用独立的 Lucide `folder-open` 图标，避免复用主页图标造成尺寸过大和裁剪观感。

## 1.1.0

- 为每条最近项目增加“置顶/取消置顶”操作，置顶状态由 Flow 持久保存。
- 置顶项目固定显示在列表顶部，即使不再出现在 Windows 最近项目记录中也会保留。
- 为每条最近项目增加“打开所在位置”操作；文件会在资源管理器中被选中，文件夹会直接打开。

## 1.0.1

- 每条结果改用目标文件或文件夹路径加载 Windows 缩略图/类型图标。
- 增加缩略图来源冒烟测试。

## 1.0.0

- 首个可用版本。
- 空白主页显示最近 15 个文件和文件夹。
- 支持 `rf 关键词` 筛选。
- 支持打开目标和复制路径。
- 解析 Windows 最近项目中的 `.lnk` 快捷方式。
