# 更新记录

## 1.2.5

- 空白主页恢复显示 Windows 最近使用的文件和文件夹，并始终将固定项目排在前面。
- 主页先立即返回内存缓存，再后台刷新 Windows Recent；仅在内容变化时重新查询，兼顾实时性与显示稳定性。
- 搜索逻辑保持独立，不受主页后台刷新影响。

## 1.2.4

- 插件作者显示由 `SHJ` 统一调整为 `lei`。

## 1.2.3

- 主页固定从仅支持文件夹扩展为同时支持文件和文件夹。
- 固定菜单名称缩短为“固定到主页 / 从主页移除”，不再包含插件名称。
- 固定文件继续支持直接打开、复制路径和“打开所在位置”。

## 1.2.2

- 恢复全局搜索关键词 `*`，直接输入时仍由 History Box 搜索最近项目，不会切换成只有文件管理扩展结果的页面。
- 保留 `rf` 显式入口，空白主页仍只显示固定文件夹。
- 搜索结果增加 `History Box 搜索` 来源标记，并提高稳定基础分，优先展示本插件的最近项目。
- 继续使用主页/搜索独立记录键，避免与文件管理扩展共享点击排序状态。

## 1.2.1

- 空白主页只显示插件自己固定的有效文件夹，不再扫描并混入全部最近项目。
- 主页固定菜单改为“固定到 History Box 主页 / 从 History Box 主页移除”，与 Flow 的当前搜索置顶明确区分。
- 主页查询改为从内存快照立即返回，减少多个主页插件异步返回时的显示抖动。
- 主页和 `rf` 搜索使用独立、带插件命名空间的 `RecordKey`，避免污染文件管理扩展的点击排序。
- 规范化路径并按路径去重；插件主页固定仅接受文件夹。
- 主页和搜索逻辑隔离，为后续恢复全局搜索做好准备。

## 1.2.0

- 插件显示名称改为 `History Box`，保持插件 ID 不变。
- 恢复插件自己的持久置顶与取消置顶，不显示成功提示，并真正固定到主页列表顶部。
- 文件夹继续通过整行操作直接打开，插件菜单不再显示“打开所在位置”。
- 普通文件提供“打开所在位置”，并在文件资源管理器中选中文件。
- 置顶和取消置顶分别使用 Lucide `pin`、`pin-off` 图标。

## 1.1.1

- 移除与 Flow 内置“在当前查询中置顶”重复的插件置顶操作。
- “打开所在位置”改用独立的 Lucide `folder-open` 图标，避免复用主页图标造成尺寸过大和裁剪观感。

## 1.1.0

- 为每条最近项目增加“置顶/取消置顶”操作，置顶状态由 Flow 持久保存。
- 置顶项目固定显示在列表顶部，即使不再出现在 Windows 最近项目记录中也会保留。
- 为每条最近项目增加“打开所在位置”操作；文件会在资源管理器中被选中，文件夹会直接打开。

## 1.0.1

- 每条结果改用目标文件或文件夹路径加载 Windows 缩略图/类型图标。
- 增加缩略图来源冒烟测试。

## 1.0.0

- 首个可用版本。
- 空白主页显示最近 15 个文件和文件夹。
- 支持 `rf 关键词` 筛选。
- 支持打开目标和复制路径。
- 解析 Windows 最近项目中的 `.lnk` 快捷方式。
